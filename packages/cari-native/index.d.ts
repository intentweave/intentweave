/**
 * Returns the absolute path to the `cari-build` native binary for the
 * current platform, or `null` if the platform-specific optional package
 * is not installed.
 */
export declare function getBinaryPath(): string | null;
